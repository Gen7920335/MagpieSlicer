# Magpie Slicer 2.5.0.0.9 Beta 1

0.8의 누적 기능에 선택형 CUDA 연산과 상세 시간 기록 설정을 추가한 Windows x64 베타다.

## 변경 사항

- 상단 Vulkan 선택 옆과 환경설정 → 그래픽에 CUDA 끄기/켜기를 추가했다. CUDA 기본값은 끄기이며 CUDA와 Vulkan은 동시에 활성화하지 않는다. 변경은 다음 슬라이싱 요청에 적용된다.
- CUDA의 다섯 연산은 수직 교차, AABB 후보 검색, 점-선분 거리, 메시 레이어 범위, 점의 다각형 포함 검사다. 결과를 CPU 기준과 전부 대조하고 실패하면 배치 전체를 CPU로 처리한다. 모든 슬라이싱을 GPU에서 수행하는 구현은 아니다.
- GT 1030(sm_61) 및 RTX 4070 SUPER(sm_89)용 커널을 포함한다. 사용자 PC에 CUDA Toolkit을 설치하도록 요구하거나 드라이버 DLL을 설치파일에 동봉하지 않는다. CUDA 사용에는 호환 NVIDIA 드라이버가 필요하며 사용할 수 없으면 CPU로 처리한다.
- 시간 기록 끄기/주요 단계/상세, JSON 자동 저장, 상세 보기 및 수동 내보내기를 제공한다. 기본 기록 수준은 상세, 자동 저장은 꺼짐이다. 기록을 꺼도 기존 전체 슬라이싱 시간 표시는 유지한다.
- 단계별 분포, GPU 전송 바이트, 초기화·대기·연산·전송·검증·CPU 대체 시간과 진단을 구분한다. 미측정값은 null이고 병렬·중첩 시간은 단순 합산하지 않는다. 이벤트 한도 초과 건수도 기록한다.
- 가속이나 기록 수준 변경 후 다음 슬라이싱에서 기존 파생 캐시를 다시 계산하도록 연결했다. 원본 모델과 G-code 전용 플레이트는 보존한다.
- 새 CUDA 및 시간 기록 UI의 한국어 번역 16개를 설치 리소스에 포함한다.
- Mixed가 Tree 채널에 수요를 배정했지만 Tree 생성기가 출력 가능한 가지를 만들지 못하면, 해당 수요를 선택된 Prusa/Cura 일반 서포트로 다시 생성한다. Tree 결과가 비었다는 이유만으로 전체 슬라이스를 중단하지 않는다.

## 검증 범위와 제한

개발 빌드의 전체 CTest 405개 항목은 실패 0, 의도적 Resin skip 1개였다. 강화된 Stanford Bunny 60도 서포트 기능 행렬은 28/28 통과했다. 최종 Mixed fallback 수정 뒤 릴리스 준비 Core 12/12, 직접 실패 재현 Mixed 4/4, 다중 재료 Mixed 1/1을 다시 통과했다. 설치용 CUDA 자체 시험은 독립 검사 196,611건, 변경 입력 1,000배치와 Max GPU 강제 경로를 통과했다.

최종 NSIS 설치파일은 15,250개 항목 무결성 검사를 통과했다. 추출한 `magpie-slicer.exe`와 `MagpieSlicer.dll`은 Release 산출물과 SHA-256이 일치하며, 패키지 실행파일의 FileVersion과 ProductVersion은 모두 `2.5.0.0.9`다.

- 파일명: `MagpieSlicer_Windows_Installer_V2.5.0.0.9_x64.exe`
- 크기: `142,233,758 bytes`
- SHA-256: `E1F602592506A648E9563858DB9DABB5546E844B025402665B827D703A159BC1`

실제 설치 및 GUI 조작, RTX 4070 SUPER 실측, 최종 Mixed fallback 수정 이후의 전체 CTest 및 전체 Bunny 28건 재실행, 전체 서포트 지지성·연결성·충돌과 실물 출력은 검증 완료로 보고하지 않는다. 현재 호스트의 GT 1030 결과만으로 4070의 속도 향상을 주장하지 않는다. CUDA 연산별 빠른 경로 자동 선정도 아직 아니다.

포터블 배포본은 제공하지 않는다. 이 빌드는 별도 Git 태그와 GitHub pre-release로 배포한다.
